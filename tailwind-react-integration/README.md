# Tailwind React Integration

A small Vite + React project demonstrating Tailwind CSS integration and a styled `UserProfile` component.

To run locally:

```bash
cd tailwind-react-integration
npm install
npm run dev
```
