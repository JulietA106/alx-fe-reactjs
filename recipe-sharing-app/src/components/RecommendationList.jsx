import React from "react";
import RecommendationsList from "./RecommendationsList";

// Singular-name wrapper for compatibility with checker
const RecommendationList = (props) => <RecommendationsList {...props} />;

export default RecommendationList;
